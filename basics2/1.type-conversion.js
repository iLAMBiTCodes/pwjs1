// Data Type Conversion

// Declare a string with number
let bankBalance = "100"
let bankBalanceinInt = parseFloat(bankBalance)
console.log(bankBalanceinInt)
console.log(bankBalance)
// The two outputs will be of different colors
// We need to find a way to find a particular data type as from the outputs above data type cannot be identified
console.log(typeof bankBalanceinInt)
console.log(typeof bankBalance)
// It shows the data type